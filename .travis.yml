languaje: node_js
node_js:
    - 13
    
service:
    - docker
    
install:
    - echo "nothing to be installed"
    
before_script:
    - echo "no test"
    
script:
    - docker --version
    - docker build -t simple-node .
    - docker tag simple-node jmespartero/simple-node_latest

after_success:
    - echo "success"
    - docker push jmespartero/simple-node_latest